// Phase 16 — Notifications has no inbound DTOs (creation is internal,
// reads use simple query params, mutations use route params only). This
// file exists so the module shape mirrors siblings; it can grow if the
// public API ever accepts user-authored notifications.
export {};
